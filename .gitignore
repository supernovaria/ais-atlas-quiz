# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Question-generation pipeline.
# runs/ is the audit trail the whole plan exists to produce — it is committed,
# and run.log must survive the *.log rule above.
!runs/**/*.log
multi-agent-generation-setup.zip
